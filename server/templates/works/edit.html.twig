{% extends 'base.html.twig' %}

{% block title %}Edition de {{ work.title }}{% endblock %}

{% block body %}
    <div class="btn btn-danger container-fluid">Mode Administration</div>
    <div class="container">
        <h1>Edition de {{ work.title }}</h1>
    {% spaceless %}
        {{ form_start(form) }}
        <div class="form_row">
            {{ form_label(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
            {{ form_help(form) }}
        </div>
        {{ form_end(form) }}
    {% endspaceless %}
    </div>
{% endblock %}